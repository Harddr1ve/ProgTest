name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: dist
      run: ls -al
    - name: enter
      run: cd TestProg/
    - name: enter
      run: cd TestProg/
    - name: enter
      run: ls -al
    - name: build 
      run: g++ -g $(find RootFolderName -type f -iregex ".\*.cpp") -o OutputName
      
